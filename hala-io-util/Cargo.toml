[package]
description = "Hala io utility tools"
documentation = "https://docs.rs/hala-io-util"
edition.workspace = true
license = "MIT"
name = "hala-io-util"
repository.workspace = true
version.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
boxcar = {workspace = true, optional = true}
bytes = {workspace = true, optional = true}
dashmap = {workspace = true, optional = true}
futures = {workspace = true}
hala-io-driver = {workspace = true}
log = {workspace = true}
pretty_env_logger = {workspace = true}

[dev-dependencies]
futures = {workspace = true}
hala-test = {workspace = true}

[features]
async_io = []
current = []
default = ["async_io", "io_group", "mux", "timeout", "read_buf", "test", "current", "poll"]
io_group = []
mux = []
poll = ["dashmap", "boxcar"]
read_buf = ["bytes"]
test = []
timeout = []
